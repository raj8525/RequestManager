# Repository Workflow

- At the end of every completed iteration, run the relevant verification, stage only the files for that iteration, create an intentional Git commit, and push the current branch to `origin`.
- Do not commit credentials, runtime data, browser artifacts, or other secrets. If a push fails, report the failure rather than treating the iteration as fully delivered.
